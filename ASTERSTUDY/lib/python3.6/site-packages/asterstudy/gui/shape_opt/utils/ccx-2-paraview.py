import logging
import ccx2paraview
import sys
logging.basicConfig(level=logging.INFO, format='%(levelname)s: %(message)s')
frd_file_name = [sys.argv[1]]
for i in frd_file_name:
    c = ccx2paraview.Converter(i, ['vtu'])
    c.run()
